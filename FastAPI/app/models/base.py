from app.db.database import Base

# Aquí las clases de SQLAlchemy heredarán de Base.
# Ejemplo:
# class Usuario(Base):
#     __tablename__ = "usuarios"
#     id = Column(Integer, primary_key=True, index=True)
